version: 2

project_name: bubbleMonitor

before:
  hooks:
    - go generate ./...

builds:
  - id: bubbleMonitor
    main: ./cmd/bub
    binary: bub
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    overrides:
      - goos: linux
        goarch: amd64
        env:
          - CGO_ENABLED=1
    hooks:
      post:
        - cp "{{ .Path }}" dist/bub_{{ .Os }}_{{ .Arch }}
        - chmod +x dist/bub_{{ .Os }}_{{ .Arch }}
    ldflags:
      - -s -w
      - -X main.buildVersion={{.Version}}
      - -X main.buildCommit={{.Commit}}
      - -X main.buildDate={{.Date}}

archives:
  - id: binaries
    format: binary
    name_template: "bub_{{ .Os }}_{{ .Arch }}"
  - id: archives
    formats:
      - tar.gz
      - zip
    wrap_in_directory: false
    name_template: "bub_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

release:
  github:
    owner: N1xev
    name: bubbleMonitor
  draft: false
  prerelease: auto
  name_template: "{{.Tag}}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^refactor:"
